name: Test Workflow for getting Github data

on:
  issues:

jobs:
  info:
    runs-on: ubuntu-latest

    steps:
      - name: Output Github context  
        run: echo "${{ toJSON(github) }}" > event.json

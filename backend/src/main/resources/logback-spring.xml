<configuration>
    <property name="LOG_DIR" value="logs"/>

    <!-- General app logs (daily rotation, 10 days) -->
    <appender name="APP" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Performance/AOP logs only (daily rotation, 10 days) -->
    <appender name="PERF" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/performance.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/performance-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Root → general app log -->
    <root level="INFO">
        <appender-ref ref="APP"/>
    </root>

    <!-- Send ONLY AOP timing logs to performance.log -->
    <!-- Use the class name for just this aspect, or the package for all AOP logs -->
    <logger name="com.example.todoapp.aop.ExecutionTimeAspect" level="INFO" additivity="false">
        <appender-ref ref="PERF"/>
    </logger>
</configuration>